# sops 加密规则
# vm 的 age 公钥由 `age-keygen` 生成后填入下方占位符。
keys:
  - &vm age1msvddvd993muthdfjydy0lkvhyx9dx920xrsmgh4w6efu67andusht7gmk
creation_rules:
  # 匹配 secrets/ 下的 yaml，统一用 vm 的 age 公钥加密
  - path_regex: secrets/.*\.ya?ml$
    key_groups:
      - age:
          - *vm
